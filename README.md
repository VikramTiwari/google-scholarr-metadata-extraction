# google scholar metadata extraction

## Setup

- Install [nodejs 12](https://nodejs.org/en/) on your machine
- Clone the project
- Go to project directory and run `npm install`

## Running the project

Project starts with `startURL`
